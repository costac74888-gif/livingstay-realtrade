# 숙박·건물 데이터 수집 및 관리 정책 V.02

**작성일:** 2026-09-03  
**적용 대상:** 숙박업 원장, 건물 마스터, 숙박·건물 관련 API 동기화, 관리자 데이터 동기화, 개발→운영 데이터 승격

> V.02 변경사항: `숙박업 기타`의 서비스 분류를 `일반숙박`으로 통일하고, 관광호텔·휴양콘도미니엄업은 별도 서비스 용도를 만들지 않고 `관광숙박`에 포함한다. 운영 관리자 화면의 직접 수집·전체 백필 버튼은 숨기지 않고 비활성화하며, `개발에서 동기화 중` 상태를 표시한다.

## 최우선 원칙. 무중단·무회귀·즉시복구

이 원칙은 이 문서의 다른 모든 정책보다 우선한다.

> **DB 정책 변경 중 기존 서비스의 모든 사용자 기능과 정상 운영 기능을 정지시키지 않는다. 정책 적용 완료 후에도 기존 기능이 이전과 동일하게 동작해야 하며, 문제가 발생하면 즉시 변경을 격리하고 마지막 정상 상태로 복구할 수 있어야 한다.**

반드시 지킬 세부 원칙:

1. 데이터 정책 변경 때문에 지도, 검색, 건물 상세, 실거래 조회, 숙박 목록·통계, 관심단지, 매물·파트너, 사진, 인증, 알림 등 기존 사용자 기능을 중단하지 않는다.
2. 기존 DB 데이터를 먼저 백업·검증하고, 파괴적 스키마 변경·대량 삭제·대량 상태 변경을 선행하지 않는다.
3. 새 수집·검증 경로는 기존 경로와 분리된 staging 또는 shadow 단계에서 먼저 실행한다.
4. 기존 운영 동기화와 대체 경로가 모두 검증되기 전에는 기존 운영 동기화나 관리자 기능을 끄지 않는다.
5. 운영 관리자 버튼을 비활성화하는 경우에도 개발/staging 대체 경로가 정상 실행 중이고 데이터 갱신 공백이 없다는 사실을 먼저 확인한다.
6. 단계별로 smoke·API·핵심 사용자 흐름·관리자 흐름을 확인하고, 하나라도 실패하면 다음 단계로 진행하지 않는다.
7. 적용 완료 후 일정한 관찰 기간 동안 오류·누락·중복·성능 저하가 없음을 확인하기 전에는 기존 경로를 제거하지 않는다.
8. 문제가 발생하면 원인을 확인하는 동안에도 사용자 서비스를 유지하고, 해당 배치·기능만 격리한 뒤 마지막 성공 배치 또는 마지막 정상 버전으로 복구한다.

정책 변경의 성공 기준은 “새 데이터 구조가 반영되었다”가 아니라 다음을 모두 만족하는 것이다.

```text
기존 기능 무중단
        +
정책에 맞는 데이터 반영
        +
완료 후 기존 동작 유지
        +
문제 발생 시 즉시 복구 가능
```

## 0. 현재 구현 상태

이 문서는 DB 정책과 단계별 실행계획을 확정하기 위한 문서다. 문서 V.02 작성 시점에는 다음 실제 변경을 아직 시작하지 않았다.

- DB 스키마 변경 없음
- 기존 `lodging_registry` 데이터 변경 없음
- 정부 CSV 신규 적재 없음
- 운영 DB 반영 없음
- 관리자 동기화 버튼 동작 변경 없음
- 운영 동기화 워크플로 중단 없음

따라서 아래 실행계획은 승인 후 단계별로 진행하며, 각 단계의 검증을 통과하기 전에는 다음 단계로 넘어가지 않는다.

## 1. 정책 목적

정부 원본이 갱신될 때마다 개발 환경에서 먼저 검증하고, 검증을 통과한 승인 배지만 운영 DB에 반영한다.

이 정책의 우선순위는 다음과 같다.

1. **최우선:** 정책 변경 중·후에 기존 모든 기능을 정지시키지 않고, 완료 후 회귀가 없도록 하며, 문제 발생 시 즉시 복구한다.
2. 정부 원본의 인허가 이력과 상태 이력을 보존한다.
3. 개발 DB와 운영 DB를 무조건 통째로 복사하지 않는다.
4. 운영 DB에는 검증·승인된 변경 배지만 반영한다.
5. 신고 데이터, 건축물 용도, 지도 좌표, 상가 정보, 사진 정보를 서로 다른 역할로 관리한다.

## 2. 정부 CSV 고정 범위

서비스가 수집·관리하는 정부 공공 CSV는 다음 8종으로 고정한다.

| 번호 | 원본 |
|---:|---|
| 1 | 관광숙박업 |
| 2 | 관광펜션업 |
| 3 | 농어촌민박업 |
| 4 | 숙박업 |
| 5 | 외국인관광도시민박업 |
| 6 | 일반야영장업 |
| 7 | 자동차야영장업 |
| 8 | 한옥체험업 |

### 제외 원본

- 전문휴양업은 서비스 숙박 원장 대상에서 제외한다.
- 원본 ZIP에 전문휴양업이 포함되어 있더라도 적재 대상 8종으로 취급하지 않는다.

## 3. CSV 원장 보존 범위

### 3.1 관광숙박업

`문화_관광숙박업.csv`의 유효한 관리번호는 전체 보존한다.

- 영업·정상 자료
- 휴업 자료
- 폐업·취소 자료
- 현재 건물과 자동 매칭되지 않는 자료

단, 유효한 관리번호가 없거나 필수 식별정보가 훼손된 행은 원본 오류·보류 행으로 따로 기록하고 서비스 원장에는 임의 등록하지 않는다.

### 3.2 `문화_숙박업.csv` 업태별 처리

| 업태 | 원장 | 서비스 숙박분류 | 기본 영업목록 | 비고 |
|---|---|---|---|---|
| 숙박업(생활) | 포함 | 생활숙박 | 표시 조건에 따름 | 기존 대상 |
| 여관업 | 포함 | 일반숙박 | 표시 조건에 따름 | 기존 대상 |
| 여인숙업 | 포함 | 일반숙박 | 표시 조건에 따름 | 기존 대상 |
| 일반호텔 | 포함 | 일반숙박 | 표시 조건에 따름 | 기존 대상 |
| 관광호텔 | 포함 | **관광숙박** | 표시 조건에 따름 | 관광숙박업 CSV와 중복 가능 |
| 휴양콘도미니엄업 | 포함 | **관광숙박** | 표시 조건에 따름 | 관광숙박업 CSV와 중복 가능 |
| 숙박업 기타 | 포함 | **일반숙박** | 표시 조건에 따름 | 일반숙박으로 통합 |
| 업태 공백 | 보류 보존 | 미분류 | 기본 제외·관리자 확인 | 원본 행은 잃지 않음 |

`관광호텔`과 `휴양콘도미니엄업`은 서비스 화면과 통계에서 별도 서비스 용도를 만들지 않고 `관광숙박`에 포함한다. 다만 정부 원본 업태인 `관광호텔`, `휴양콘도미니엄업`은 원장과 상세 화면에서 원문 업태로 표시할 수 있다.

`숙박업 기타`는 원장에 보존하되 서비스 숙박분류는 `일반숙박`으로 지정한다. 기존의 `기타숙박` 서비스 분류를 새로 만들거나 유지하지 않으며, 원문 업태를 별도 상세 정보로 보존한다.

`관광숙박`, `일반숙박`, `생활숙박`, `미분류`는 서비스 분류이다. 건축물대장의 `building_use_type` 또는 원문 건축물 용도를 자동으로 변경한다는 뜻이 아니다.

### 3.3 복수 인허가

동일 주소·동일 건물에 여러 인허가가 있더라도 관리번호가 다르면 각각의 신고를 보존한다.

- 건물 상세: 인허가별로 모두 표시
- 관리자 원장: 인허가별 행으로 관리
- 신고 건수: 관리번호 기준
- 실제 사업장 수: 별도 중복 분석 기준
- 연결 건물 수: 건물 ID 기준

주소가 같다는 이유만으로 서로 다른 인허가를 합치거나 삭제하지 않는다.

## 4. 영업상태 정책

| 상태 | 원장 | 기본 영업목록 | 활성 영업 통계 | 이력 조회 |
|---|---|---|---|---|
| 영업/정상 | 보존 | 표시 | 포함 | 표시 |
| 휴업 | 보존 | **표시** | 제외 또는 별도 집계 | 표시 |
| 폐업 | 보존 | 기본 제외 | 제외 | 표시 |
| 취소·말소 | 보존 | 기본 제외 | 제외 | 표시 |
| 상태 불명·오류 | 보류 보존 | 기본 제외 | 제외 | 관리자 확인 |

휴업은 기본 영업목록에 표시하되, 화면에서 `휴업` 배지를 명확히 표시한다. 영업 중 사업장 수·신고율 등 활성 통계에는 포함하지 않고 `휴업 수`를 별도로 집계한다.

폐업·취소·말소 자료는 물리 삭제하지 않는다. 최신 전체 원본에서 행이 사라졌다는 이유만으로 즉시 폐업 처리하지 않으며, 완전 수집이 확인된 경우에만 상태 변경 후보로 만든다.

## 5. 데이터 출처별 역할

API가 서로 다른 사실을 제공하므로 모든 API를 같은 합격 조건으로 사용하지 않는다.

### 5.1 숙박 신고의 1차 기준

- 8종 정부 CSV
- 행정안전부 일반·생활숙박 API
- 농어촌민박·한옥체험업·관광펜션업 API
- GoCamping API

관리번호·사업장명·주소·인허가일·영업상태를 검증한다.

### 5.2 건물 연결·건축정보

- 건축물대장 HUB 표제부
- 층별개요·전유공용면적 등 건축물대장 세부 API
- 건축인허가 API
- JUSO 주소 API
- 카카오 주소·좌표 API

이 자료는 숙박 신고가 어느 건물에 연결되는지와 건축물 정보를 확인하는 데 사용한다. 건축물대장 용도가 숙박시설이 아니라는 이유만으로 유효한 정부 영업신고를 삭제하지 않는다.

### 5.3 거래·부가정보

- 국토교통부 실거래가 API: 거래 데이터
- 소상공인 상가정보 API: 건물 내 상가·부동산 정보
- 공인중개사 표준데이터 API: 중개업소 정보

이 자료는 숙박 인허가의 유효성을 단독으로 판정하지 않는다.

### 5.4 지도·사진

- 카카오 Maps JS SDK: 지도 화면 표시
- TourAPI: 관광·숙박 사진
- Google Street View: 사진 fallback
- VWorld: 항공·공간 이미지 보조

사진 또는 좌표 API가 실패해도 정부 숙박 원장을 불합격·삭제하지 않는다.

### 5.5 지도 범례 구분

지도 범례의 사용자 노출 구분은 정책 V.02 적용 전과 동일하게 유지한다. 원장 내부의 정부 원문 업태와 서비스 분류를 정리하더라도 범례를 관광호텔·휴양콘도미니엄업 등으로 세분화하거나 새 색상 항목을 추가하지 않는다.

현재 범례 항목:

- 생활숙박
- 관광숙박
- 일반숙박
- 에어비앤비
- 농어촌민박
- 캠핑·야영
- 한옥
- 복합
- 준공전
- 미분류

관광호텔과 휴양콘도미니엄업은 `관광숙박` 건수에 포함하고, 숙박업 기타는 `일반숙박` 건수에 포함한다. 정부 원문 업태는 건물 상세·관리자 화면 등 필요한 위치에서만 별도로 표시할 수 있다.

## 6. 개발 검증→운영 승격 절차

### 6.1 원본 등록

관리자가 새 CSV를 업로드하면 다음을 기록한다.

- 원본 파일명
- 파일 해시
- 원본 기준일
- 업로드 시각
- 원본 종류
- 행 수
- 파일 인코딩·파싱 결과

### 6.2 개발/staging 적재

원본은 먼저 staging 영역에 적재한다. 기존 운영 원장을 바로 수정하지 않는다.

검증 결과에는 다음을 포함한다.

- 총 행 수
- 유효 행 수
- 필수값 오류
- 관리번호 중복
- 기존 관리번호
- 신규 행
- 변경 행
- 상태 변경 후보
- 주소 미매칭
- 건물 연결 후보
- API 호출 실패

### 6.3 상호검증

숙박 신고 원장의 합격 판단은 다음 순서로 한다.

1. CSV 필수 식별정보 검증
2. 동일 원본 내부 중복 제거
3. 동일 관리번호 기준 기존 DB 비교
4. 정부 숙박 API와 상태·주소·사업장명 비교
5. JUSO·카카오를 이용한 주소 보정
6. 건축물대장과 건물 연결 후보 확인
7. 충돌·미매칭·API 실패를 별도 보류로 분리

카카오 사진, Street View, 상가정보가 없다는 이유로 숙박 신고를 불합격시키지 않는다.

### 6.4 승인 배치 생성

검증이 끝나면 전체 개발 DB가 아니라 다음 변경분만 승인 배치로 고정한다.

- 추가
- 수정
- 휴업 전환
- 폐업·취소 전환 후보
- 건물 연결·연결 해제 후보
- 보류·관리자 확인 필요

승인 배치에는 실행번호, 원본 해시, 생성 시각, 검증 결과를 기록한다.

### 6.5 운영 반영

운영 DB에는 승인된 배지만 반영한다.

- 개발 DB 전체를 운영 DB에 덮어쓰지 않는다.
- 운영에서 같은 원본을 독립적으로 다시 호출해 다른 결과를 만들지 않는다.
- 반영 전 dry-run을 실행한다.
- 반영은 관리번호·건물 ID 등 논리적 키 기준으로 멱등 처리한다.
- 반영 실패 시 같은 실행번호로 재시도할 수 있어야 한다.
- 기존 건물 연결은 새 매칭이 확정되지 않으면 보존한다.

## 7. 관리자 동기화 버튼 운영 원칙

운영 관리자 화면에서는 개발/staging으로 이전되는 기능을 숨기지 않는다. 운영자가 현재 상태와 전환 대상을 알 수 있도록 버튼과 기능 설명을 그대로 표시하되, 운영 DB에 직접 영향을 줄 수 있는 대상은 비활성화한다.

다음 버튼은 운영 화면에 계속 표시하지만 실행할 수 없도록 한다.

| 운영 화면 버튼 | 운영 상태 | 표시 문구 | 실제 실행 위치 |
|---|---|---|---|
| 숙박 API 직접 동기화 | 비활성화 | 개발에서 동기화 중 | 개발/staging |
| 캠핑 API 직접 동기화 | 비활성화 | 개발에서 동기화 중 | 개발/staging |
| 농어촌민박 API 직접 동기화 | 비활성화 | 개발에서 동기화 중 | 개발/staging |
| 한옥체험업 API 직접 동기화 | 비활성화 | 개발에서 동기화 중 | 개발/staging |
| 관광펜션 API 직접 동기화 | 비활성화 | 개발에서 동기화 중 | 개발/staging |
| 원본 CSV 직접 승인 반영 | 비활성화 | 개발에서 검증·승인 배치 생성 중 | 개발/staging |
| 건축물대장 전체 재수집 | 비활성화 | 개발에서 동기화 중 | 개발/staging |
| 실거래 전체 백필 | 비활성화 | 개발에서 동기화 중 | 개발/staging |

비활성화된 버튼에는 다음 정보를 함께 표시한다.

- 현재 기능은 운영 DB에 직접 반영되지 않음
- 개발/staging에서 동기화·검증 중임
- 운영 반영은 승인 배치 절차를 통해 진행함
- 현재 실행 중인 배치와 마지막 완료 시각

운영 화면에서 비활성화 상태를 우회하여 직접 호출하는 API도 허용하지 않는다. 화면의 `disabled` 속성만으로 보호하지 말고 서버 라우트에서 운영 환경·실행 종류·승인 배치를 함께 확인한다.

단, 새 개발/staging 동기화와 승인 배치가 안정적으로 확인되기 전까지 기존 운영 데이터의 갱신 자체를 중단하지 않는다. 전환 기간에는 기존 작업을 안전 모드로 유지하되, 위 버튼은 새 직접 실행을 시작하지 못하도록 한다.

## 8. 정기 동기화 운영 원칙

현재 운영 DB를 직접 갱신하는 API 워크플로는 즉시 중단하지 않는다. 먼저 새 개발 검증·승격 흐름을 병행 검증한 뒤, 결과가 안정되면 운영 직접 수집을 단계적으로 중단한다.

최종 목표는 다음과 같다.

```text
정부 원본/API 1회 수집
        ↓
개발/staging 검증
        ↓
승인 배치 생성
        ↓
운영 DB 반영
        ↓
동일 배치번호·결과 확인
```

운영 직접 수집을 제거하기 전까지는 기존 운영 데이터와 새 승인 배치가 충돌하지 않도록 소스별 잠금·실행번호·체크포인트를 사용한다.

## 9. 현재 기능 보호 기준

다음 기능은 데이터 정책 변경 중에도 기존 동작을 유지해야 한다.

- 지도·건물 검색
- 건물 상세
- 실거래 조회
- 숙박 신고율·숙박 목록
- 관심단지
- 매물·파트너 노출
- 건물 사진·TourAPI·Street View fallback
- 관리자 동기화 화면 및 진행상태 조회
- 운영 관리자 화면의 전환 대상 버튼 표시
- 전환 대상 버튼의 비활성화 및 `개발에서 동기화 중` 상태 표시

대규모 적재 전에는 smoke/API 테스트와 관리자 동기화 테스트를 실행한다. 사진·지도·인증·매물 기능과 무관한 숙박 원장 확장 때문에 기존 API 응답 필드나 상태값을 임의로 변경하지 않는다.

## 10. 삭제·비활성화 안전장치

다음 조건이 충족되지 않으면 자동 삭제하지 않는다.

- 최신 원본의 전국 또는 전체 지역 수집이 완료됨
- API 호출 실패·페이지 누락이 없음
- 실행번호와 원본 기준일이 기록됨
- 기존 행과 신규 행의 관리번호 비교가 완료됨
- 폐업·취소 상태를 원본에서 확인함

조건이 충족되지 않으면 해당 행은 `보류` 또는 `확인 필요`로 남긴다.

## 11. 예상 원장 규모

2026-09-03 기준 양쪽 DB의 유효 데이터와 새 8종 CSV를 합리적으로 정리한다는 가정에서:

- `문화_숙박업.csv`의 기존 4개 업태만 포함: 약 139,000건
- 관광호텔·휴양콘도미니엄업·숙박업 기타까지 원장에 포함: 약 **143,500~143,800건**

이 숫자는 원본 행 수가 아니라 관리번호 오류·완전 중복·파싱 오류를 제외한 예상 범위다. 실제 운영 반영 건수는 개발 staging dry-run 이후 확정한다.

신고 원장 총건수와 다음 수치는 별도로 관리한다.

- 활성 영업 신고 수
- 휴업 신고 수
- 폐업·취소 이력 수
- 실제 사업장 추정 수
- 연결 건물 수
- 건물 미연결 신고 수

## 12. 정책 변경 원칙

이 문서의 8종 범위, 전문휴양업 제외, 관광호텔·휴양콘도미니엄업의 `관광숙박` 포함, `숙박업 기타`의 `일반숙박` 통합, 복수 인허가 보존, 휴업 기본목록 표시, 폐업 원장 보존, 운영 직접 동기화 버튼 비활성화 원칙은 관리자 동기화·통계·화면·운영 승격 절차에 공통 적용한다.

정책을 변경할 때는 기존 데이터 삭제보다:

1. 새 분류 추가
2. 기존 원본·관리번호 보존
3. 통계·목록 표시를 분리
4. dry-run 결과 확인
5. 승인 배치 반영

운영 관리자 화면의 전환 대상 동기화 기능은 숨기지 않고 비활성화한다. 비활성화 사유는 `개발에서 동기화 중`으로 표시하며, 개발/staging 검증과 운영 승인 배치 반영이 안정화된 뒤에만 운영 실행 권한을 재검토한다.

순서로 처리한다.

## 13. V.02 단계별 실행계획

### 단계 0. 무중단 안전망·기준선 고정

**목적:** 정책 변경 중과 완료 후에도 기존 기능을 계속 제공할 수 있는 안전망을 먼저 만든다.

실행 항목:

1. 개발·운영 DB의 `lodging_registry`, `master_buildings`, `transactions` 건수와 상태별 건수를 기록한다.
2. 관리번호·원본 종류·주소·영업상태별 중복 현황을 추출한다.
3. 현재 관리자 버튼과 각 API 라우트·실행 스크립트·대상 DB를 매칭한다.
4. 현재 운영 자동 동기화 워크플로의 마지막 성공 시각과 실행 대상을 기록한다.
5. 사용자 핵심 흐름과 관리자 핵심 흐름을 기준선 테스트로 고정한다.
6. 정책 변경 전 백업·복구 절차와 복구 대상 배치 범위를 확인한다.
7. 정책 변경 중 기존 데이터 삭제·대량 비활성화·기존 동기화 중단을 금지한다.

**통과 조건:**

- 변경 전 기준 리포트가 저장됨
- 운영 직접 변경 경로가 목록화됨
- 현재 운영 자동 동기화 상태를 확인할 수 있음
- 기존 사용자·관리자 핵심 흐름의 기준선 결과가 저장됨
- 복구 가능한 백업 또는 마지막 정상 배치가 확인됨

이 단계에서는 데이터 적재, 운영 버튼 비활성화, 운영 설정 변경을 하지 않는다.

### 단계 1. 데이터 계약과 분류 규칙 확정

**목적:** 기존 기능의 응답·필터·통계를 깨지 않으면서 importer·API·화면·통계가 같은 값을 사용하도록 데이터 계약을 먼저 고정한다.

확정할 값:

| 원본 업태 | 서비스 숙박분류 | 원문 업태 보존 | 상태 처리 |
|---|---|---|---|
| 관광호텔 | 관광숙박 | 보존 | 정상 규칙 적용 |
| 휴양콘도미니엄업 | 관광숙박 | 보존 | 정상 규칙 적용 |
| 숙박업 기타 | 일반숙박 | 보존 | 정상 규칙 적용 |
| 업태 공백 | 미분류 | 보존 | 관리자 확인 |

이 단계에서 다음을 명시적으로 금지한다.

- `기타숙박` 서비스 분류 신규 생성
- 관광호텔·휴양콘도미니엄업을 별도 서비스 용도로 분리
- `관광숙박` 분류로 `building_use_type` 덮어쓰기
- 관리번호가 다른 복수 인허가 병합

**통과 조건:**

- 서비스 분류 값과 원문 업태 값이 분리되어 있음
- `일반숙박`, `관광숙박`, `생활숙박`, `미분류`의 허용 값이 정해짐
- 휴업·폐업·취소·말소의 목록·통계 규칙이 정해짐
- 기존 API 응답 필드와 기존 화면에서 사용하는 값의 호환성이 확인됨

### 단계 2. 개발 DB에 staging·배치 구조 구축

**목적:** 원본을 운영 원장에 직접 넣지 않고, 기존 운영 서비스와 분리된 검증 가능한 배치로 만들 수 있게 한다.

실행 항목:

1. 정부 CSV 원본 파일과 파일 해시를 보존한다.
2. 8종 원본을 원본 종류별 staging 영역에 적재한다.
3. 원본 행과 정규화된 서비스 값을 함께 보존한다.
4. 관리번호·원본 종류·기준일을 배치의 논리적 키로 사용한다.
5. 검증 결과와 관리자 보류 사유를 배치에 기록한다.
6. 운영 반영 전용 승인 상태와 실행번호를 만든다.

필요한 배치 상태:

```text
uploaded
→ parsed
→ validating
→ review_required 또는 validated
→ approved
→ dry_run
→ applied 또는 failed
```

**통과 조건:**

- staging 적재만으로 운영 원장이 변경되지 않음
- 같은 원본을 다시 올려도 중복 배치가 생성되지 않음
- 실패한 배치를 같은 실행번호로 재시도할 수 있음
- 승인되지 않은 배치는 운영 반영 대상이 되지 않음
- staging 장애가 운영 서비스의 검색·상세·통계 응답을 중단시키지 않음

### 단계 3. 8종 원본 수집·검증 구현

**목적:** 정부 CSV 8종을 동일한 검증 흐름으로 처리하되, 기존 서비스 데이터를 변경하지 않는 개발 전용 경로에서 검증한다.

검증 순서:

1. 파일 형식·인코딩·필수 열 확인
2. 관리번호와 원본 종류 확인
3. 원본 내부 중복 확인
4. 업태 정규화
5. 영업상태 정규화
6. 기존 원장과 관리번호 기준 비교
7. 주소·건물 연결 후보 생성
8. 정부 숙박 API와 상호검증
9. 보류·오류·미매칭 분리
10. 승인 가능 변경분 산출

이 단계에서 건물 사진, 좌표, 상가정보, 실거래가 없다는 이유로 숙박 신고를 불합격시키지 않는다.

**통과 조건:**

- 8종별 총 행 수와 유효 행 수가 확인됨
- 관광호텔·휴양콘도미니엄업이 관광숙박으로 매핑됨
- 숙박업 기타가 일반숙박으로 매핑됨
- 공백 업태가 미분류·관리자 확인으로 남음
- 복수 관리번호가 하나로 합쳐지지 않음
- 휴업·폐업·취소·말소 행이 삭제되지 않음
- 기존 서비스 API와 화면이 기존 기준선과 동일하게 동작함
- 외부 API 실패가 기존 사용자 기능의 응답 실패로 전파되지 않음

### 단계 4. 개발 관리자 검증 화면 구축

**목적:** 운영 서비스와 분리된 개발 관리자 화면에서 운영 반영 전에 사람이 변경 내용을 확인할 수 있게 한다.

개발 화면에 제공할 항목:

- 원본 파일·기준일·해시
- 전체·신규·수정·상태변경 건수
- 관광숙박·일반숙박·생활숙박·미분류 건수
- 휴업·폐업·취소·말소 건수
- 주소 미매칭·건물 미매칭
- 복수 인허가 목록
- 보류 사유
- 운영 반영 예정 변경 목록

이 화면의 “승인 배치 생성”은 변경 배지를 만드는 단계이며, 운영 DB를 직접 수정하지 않는다.

**통과 조건:**

- 관리자가 변경 행을 원문과 비교할 수 있음
- 보류 행을 승인 배지에서 제외할 수 있음
- 승인 배지에 원본 해시·기준일·실행번호가 기록됨
- 개발 검증 화면 오류가 운영 관리자·사용자 화면에 영향을 주지 않음

### 단계 5. 운영 관리자 화면 전환

**목적:** 운영 화면은 유지하면서도, 대체 동기화가 검증된 뒤에만 운영 DB에 직접 쓰는 전환 대상 기능을 안전하게 비활성화한다.

이 단계의 선행 조건:

- 단계 0~4의 통과 조건을 모두 충족함
- 개발/staging 대체 동기화가 실제로 실행됨
- 대체 경로에서 생성한 결과를 운영에 반영할 수 있음
- 데이터 갱신 공백이 발생하지 않는 실행 일정이 확인됨
- 기존 운영 경로를 되돌릴 방법이 확인됨

선행 조건을 충족하지 못하면 운영 버튼을 비활성화하지 않는다. 운영 기능을 먼저 끄고 나중에 대체 경로를 만드는 순서는 허용하지 않는다.

운영 화면에서는 다음 버튼을 계속 표시한다.

- 숙박 API 직접 동기화
- 캠핑 API 직접 동기화
- 농어촌민박 API 직접 동기화
- 한옥체험업 API 직접 동기화
- 관광펜션 API 직접 동기화
- 원본 CSV 직접 승인 반영
- 건축물대장 전체 재수집
- 실거래 전체 백필

단, 각 버튼은 다음 상태로 표시한다.

```text
[비활성화] 개발에서 동기화 중
```

추가 표시:

- 개발/staging 마지막 동기화 시각
- 현재 실행 중인 배치
- 운영 반영 방식
- 비활성화 사유

서버에서도 운영 환경의 직접 실행 요청을 거부해야 한다. 브라우저에서 버튼을 활성화하거나 API를 직접 호출하는 방식으로 우회할 수 없어야 한다.

**통과 조건:**

- 운영 화면에서 버튼이 숨겨지지 않음
- 버튼이 클릭되지 않음
- 직접 API 호출도 실행되지 않음
- 개발 화면에서는 해당 기능이 실행됨
- 승인 배치 반영 기능은 별도 경로로 구분됨
- 버튼 전환 전후 사용자 기능과 운영 데이터 갱신이 중단되지 않음
- 비활성화 후 문제가 발생하면 기존 정상 경로로 복구할 수 있음

### 단계 6. 승인 배치 운영 반영

**목적:** 기존 운영 기능을 유지한 채 개발에서 확정한 변경분만 운영에 멱등적으로 적용한다.

반영 순서:

1. 승인 배치 선택
2. 운영 DB를 대상으로 dry-run
3. 예상 신규·수정·상태변경·보류 건수 표시
4. 관리자가 반영 확인
5. 관리번호 기준으로 적용
6. 반영 결과와 오류 저장
7. 개발·운영 결과 비교

반영하지 않는 항목:

- 보류 행
- 필수 식별정보 오류
- 전국 수집 완전성이 확인되지 않은 상태 변경
- 새 매칭이 확정되지 않은 기존 건물 연결 해제
- 승인 배지에 포함되지 않은 개발 DB 전체 데이터

**통과 조건:**

- 동일 배치를 두 번 적용해도 중복되지 않음
- 실패 지점부터 재시도 가능
- 반영 전후 건수 차이가 설명 가능함
- 기존 운영 데이터가 승인되지 않은 이유로 삭제되지 않음
- 반영 중에도 사용자 핵심 기능이 정지되지 않음
- 반영 후 사용자 핵심 기능과 관리자 기능이 기준선과 동일하게 동작함
- 오류 발생 시 해당 배치만 격리하고 마지막 정상 상태로 복구할 수 있음

### 단계 7. 병행 운영 및 전환 판정

**목적:** 기존 운영 직접 동기화를 갑자기 끄지 않고, 새 구조의 결과와 기존 서비스 기능을 함께 비교한다.

전환 기간에는 다음을 병행한다.

- 기존 운영 자동 동기화: 즉시 중단하지 않음
- 개발/staging 신규 수집·검증: 실행
- 승인 배치 dry-run: 실행
- 동일 기간의 개발 결과와 운영 결과 비교

비교 항목:

- 관리번호별 추가·수정·상태변경
- 운영과 staging의 누락
- 중복 증가 여부
- 휴업·폐업 상태 차이
- 관광숙박·일반숙박 분류 차이
- 건물 연결 변경
- 실행 소요시간·API 호출량
- 사용자 API 오류율·응답시간
- 관리자 화면 오류와 동기화 상태 표시
- 사진·지도·인증·매물 등 비동기화 기능의 정상 동작

다음 조건을 만족할 때만 기존 운영 직접 수집을 단계적으로 대체한다.

- 동일 원본에 대해 반복 검증 결과가 일관됨
- 승인 배치 반영이 멱등적으로 동작함
- 실패 배치 재시도가 가능함
- 데이터 삭제·중복·분류 오류가 없음
- 운영 서비스의 검색·상세·통계·매물 기능에 회귀가 없음
- 기존 사용자·관리자 기능이 관찰 기간 동안 중단되지 않음
- 문제 발생 시 즉시 기존 정상 경로로 복구할 수 있음

실거래는 숙박 CSV와 별도 파이프라인으로 유지한다. 실거래 전체 백필 버튼은 운영 화면에 남겨두되 비활성화하고, 개발에서 검증 중인 상태를 표시한다.

### 단계 8. 전환 완료 후 운영

최종 운영 구조는 기존 사용자 기능의 무중단을 전제로 다음과 같이 운영한다.

```text
정부 CSV/API 원본
        ↓
개발/staging 수집
        ↓
검증·보류·승인 배치
        ↓
운영 dry-run
        ↓
운영 DB 승인 배치 반영
        ↓
결과·감사 로그·통계 갱신
```

운영 반영 이후에도 다음을 정기 점검한다.

- 배치별 반영 성공 여부
- 운영·개발 건수 차이
- 원본 최신 기준일
- 휴업·폐업 상태 변화
- 미매칭 건물
- 복수 인허가 보존 여부
- 통계 중복 여부
- API 호출 한도와 실패율
- 사용자 API 오류율과 응답시간
- 핵심 화면·관리자 화면의 회귀 여부
- 마지막 정상 배치와 복구 가능 여부

## 14. 단계별 중단·복구 기준

다음 상황에서는 해당 단계의 운영 반영을 중단하고 배치를 보류한다.

- 원본 파일 해시가 기존 파일과 다르지만 기준일이 불명확함
- 필수 관리번호 누락이 평소보다 크게 증가함
- 전국 수집 중 API 페이지 누락이 발생함
- 동일 관리번호의 주소·업태·상태가 충돌함
- 예상 폐업·비활성화 건수가 비정상적으로 큼
- 건물 연결이 대량으로 해제됨
- 통계 또는 사용자 검색 결과가 기준선과 크게 달라짐
- 기존 사용자 기능 또는 관리자 기능이 오류·지연·정지됨
- 정책 적용 완료 후 기존 기능의 동작이 기준선과 달라짐

복구 원칙:

1. 사용자 서비스는 유지하고 문제가 있는 배치·경로만 격리한다.
2. 현재 운영 데이터는 즉시 삭제하지 않는다.
3. 문제가 있는 배치는 `failed` 또는 `review_required`로 남긴다.
4. 마지막 성공 배치 이후의 변경분을 식별한다.
5. 필요하면 마지막 정상 배치 또는 기존 정상 경로로 즉시 되돌린다.
6. 원본 재수집 또는 오류 수정 후 새 배치번호로 재검증한다.
7. 운영 반영 전 dry-run과 핵심 회귀 테스트를 다시 실행한다.

기존 기능이 정지되었거나 완료 후 문제가 남아 있는 상태에서는 정책 적용 완료로 선언하지 않는다.

## 15. 구현 완료 판정

V.02 정책의 실제 적용 완료는 문서 작성만으로 판정하지 않는다. 다음을 모두 확인해야 한다.

- 정책 변경 중 기존 사용자 기능이 중단되지 않음
- 정책 변경 중 기존 정상 운영·동기화 기능의 갱신 공백이 없음
- 8종 CSV가 staging에서 처리됨
- 분류 규칙이 코드·DB·화면·통계에 동일하게 적용됨
- 숙박업 기타가 일반숙박으로 처리됨
- 관광호텔·휴양콘도미니엄업이 관광숙박에 포함됨
- 원문 업태가 필요할 때 표시됨
- 휴업이 기본 목록에 표시됨
- 폐업·취소·말소가 원장에 보존됨
- 복수 인허가가 보존·표시됨
- 운영 전환 대상 버튼이 보이지만 비활성화됨
- 운영 API 직접 실행이 차단됨
- 승인 배지만 운영에 반영됨
- 실거래 전체 백필은 운영에서 비활성화되고 개발 동기화 상태가 표시됨
- smoke·API·관리자 동기화·배치 멱등성 검증이 통과됨
- 정책 적용 후 기존 기준선 테스트와 핵심 사용자 흐름이 모두 통과함
- 오류 발생 시 마지막 정상 상태로 복구하는 절차가 검증됨
- 관찰 기간 동안 기존 기능의 오류·정지·성능 저하가 없음

위 조건을 하나라도 충족하지 못하면 “정책 적용 완료” 또는 “운영 전환 완료”로 표시하지 않는다. 특히 기존 기능이 정지되었거나 완료 후 문제가 발생한 상태에서는 분류·적재가 성공했더라도 완료로 처리하지 않는다.