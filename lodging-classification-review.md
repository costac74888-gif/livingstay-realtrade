# 숙박시설 용도 분류 검토 결과

> 작성일: 2026-08-30  
> 기준: 운영 DB 읽기 전용 확인 결과 및 관리자 분류 로직 검토  
> 목적: 지도 범례의 `미분류` 수치와 관리자 재분류 대상의 차이를 설명하고, 기존 생활숙박시설 확정값을 보호하면서 나머지를 재분류하는 기준을 정리한다.

## 1. 결론 요약

1. 지도 범례의 기존 `생활숙박시설 936건`은 건축물대장 용도 판정으로 확정된 대분류이므로 유지한다.
2. 영업신고 업종명인 `숙박업(생활)`과 건축물대장 용도인 `생활숙박시설`은 같은 필드가 아니다.
3. 기존 `생숙 623건`은 건축물대장으로 확정된 생활숙박시설이 아니라, 영업신고·건물명 추정 과정에서 비확정적으로 저장된 값이다.
4. 기존 `기타 5,534건`은 법정 영업신고 업종명이 아니라, 영업신고 업종을 `lodging_type`으로 매핑하지 못해 남은 임시 분류다.
5. 운영 DB를 실제로 변경하지 않고 건물 단위로 미리 계산하면 `기타 5,534건`은 다음처럼 나뉜다.

| 예상 이동 대상 | 건수 |
|---|---:|
| 생활숙박시설 | 1,123건 |
| 일반 | 4,406건 |
| 추가 확인 | 5건 |
| 합계 | 5,534건 |

6. 기존 `생숙 623건`도 같은 우선순위를 적용하면 다음과 같다.

| 예상 이동 대상 | 건수 |
|---|---:|
| 생활숙박시설 | 216건 |
| 일반 | 407건 |
| 추가 확인 | 0건 |
| 합계 | 623건 |

따라서 두 비확정 그룹을 함께 처리할 경우, 기존 확정 `생활 936건`은 보호하고 `기타`와 비확정 `생숙`을 재분류한다.

## 2. 용어와 데이터 출처

### 2.1 `생활` 또는 `생활숙박시설`

`master_buildings.lodging_type`에 저장된 건축물 대장 기반 대분류다. 표제부·층별개요의 용도 원문을 판정하여 생활숙박시설로 확인된 건물이다.

운영 DB에서 현재 `생활` 대분류는 936건이다. `lodging_type_detail`에도 `생활숙박시설`, `숙박시설(생활숙박시설)` 등 건축물대장 원문이 저장되어 있다.

이 값은 영업신고 업종명보다 우선하는 확정 근거로 취급한다.

### 2.2 `숙박업(생활)`

`lodging_registry.hygiene_type`에 저장된 행정 영업신고 업종명이다. 건물의 건축물대장 용도와 반드시 일치하거나 일대일로 대응하지 않는다.

실제로 운영 DB에서 `숙박업(생활)` 신고가 연결된 건물은:

- 확정 `생활` 대분류 안에도 362건 존재
- 기존 `생숙` 그룹 안에도 216건 존재
- 기존 `기타` 그룹 안에도 1,123건 존재

따라서 `숙박업(생활)`이라는 신고명만 보고 모든 건물을 `일반` 또는 모든 건물을 `생활숙박시설`로 일괄 이동하면 오분류가 발생할 수 있다.

### 2.3 `생숙 623건`

현재 `master_buildings.lodging_type = '생숙'`으로 저장된 별도 값이다. 이 623건은 건축물대장으로 확정된 `생활 936건`과 별개다.

운영 DB에서 623건의 실제 연결 영업신고 업종은 다음과 같다.

| 영업신고 업종 | 건물 수 |
|---|---:|
| 여관업 | 342건 |
| 숙박업(생활) | 216건 |
| 일반호텔 | 66건 |
| 여인숙업 | 35건 |

위 숫자는 한 건물에 여러 신고가 있을 수 있으므로 단순 합계하면 623건을 초과할 수 있다. 실제로 이 그룹에서 여러 영업신고가 연결된 건물은 72건이다.

### 2.4 `기타 5,534건`

`기타`는 법정 업종이나 최종 용도명이 아니다. 영업신고 데이터가 연결되어 있지만, 당시 매핑표와 건물명 키워드 판정으로 `lodging_type`을 정하지 못했을 때 사용하는 fallback 값이다.

현재 `기타` 5,534건에는 다음 업종들이 연결되어 있다.

| 영업신고 업종 | 연결된 고유 건물 수 |
|---|---:|
| 여관업 | 3,351건 |
| 일반호텔 | 1,131건 |
| 숙박업(생활) | 1,123건 |
| 여인숙업 | 329건 |

이 표 역시 한 건물에 복수 신고가 있을 수 있으므로 합산해서 5,534건과 비교하면 안 된다. `기타` 그룹에서 복수 영업신고가 연결된 건물은 717건이다.

## 3. 지도 범례에 6,242건으로 보였던 이유

기존 지도용 분류 코드가 새 저장값 `생숙`을 별도 생활 분류로 인식하지 못했고, 별도 범례가 없는 `기타`도 `미분류`로 표시했다.

그 결과 과거 범례의 `미분류 6,242건`은 다음 값이 합쳐진 결과였다.

```text
기타 5,534건
+ 생숙 623건
+ 실제 NULL/빈값 85건
= 6,242건
```

`생숙`을 생활숙박시설 범례로 정상 인식하도록 표시 로직은 수정했다. 다만 `기타`는 아직 실제 유형이 확정되지 않았으므로 재분류 전까지는 미분류 성격으로 남는다.

## 4. `기타 5,534건` 건물 단위 미리보기

복수 신고가 연결된 건물은 다음 우선순위로 계산했다.

1. `숙박업(생활)` 신고가 하나라도 있으면 → 생활숙박시설
2. `여관업·일반호텔·여인숙업`만 있으면 → 일반
3. 영업신고 연결이 없으면 → 추가 확인

그 결과:

| 현재 값 | 생활숙박시설로 이동 | 일반으로 이동 | 추가 확인 | 합계 |
|---|---:|---:|---:|---:|
| 기타 | 1,123 | 4,406 | 5 | 5,534 |

이 결과는 DB를 변경하지 않은 읽기 전용 미리보기다.

## 5. `생숙 623건` 건물 단위 미리보기

같은 우선순위를 적용하면:

| 현재 값 | 생활숙박시설로 이동 | 일반으로 이동 | 추가 확인 | 합계 |
|---|---:|---:|---:|---:|
| 생숙 | 216 | 407 | 0 | 623 |

여기서 `216건`은 `숙박업(생활)` 신고가 있는 건물 수다. `623 - 216 = 407`이라는 계산은 이 그룹에 대해서는 건물 단위 우선순위 결과와 일치하지만, 업종별 원시 건수 전체는 복수 신고 때문에 단순 합산하면 안 된다.

## 6. 확정 처리 기준

### 유지할 데이터

- 기존 건축물대장 확정 `생활` 936건
- 이 중 `숙박업(생활)` 신고가 연결된 362건도 그대로 유지

### 재분류할 데이터

- 현재 `기타`인 건물
- 현재 `생숙`이지만 건축물대장 확정값이 아닌 건물

### 재분류 우선순위

```text
1. 건축물대장으로 확정된 생활 → 유지
2. 비확정 기타/생숙 중 숙박업(생활) 신고 존재 → 생활
3. 비확정 기타/생숙 중 여관업·일반호텔·여인숙업만 존재 → 일반
4. 근거가 없는 건물 → 추가 확인
```

이 우선순위를 사용하면 영업신고 업종만으로 이미 확정된 `생활` 936건을 일반으로 덮어쓰는 문제를 피할 수 있다.

## 7. 현재 코드 반영 상태

다음 내용은 코드에 반영되어 있다.

- 지도 집계에서 `생숙`을 `생활`로 표시
- 수집 시 `여관업·일반호텔·숙박업(생활)·여인숙업`을 일반 후보로 인식
- 관리자 영업신고 기준 재분류에서 기존 확정 `생활`은 보호
- 관리자 재분류는 현재 `기타`와 비확정 `생숙`을 대상으로만 위 업종 매핑을 적용
- 실제 운영 데이터 소급 재분류는 아직 실행하지 않음

## 8. 실행 전 주의사항

관리자에서 소급 재분류를 실행하기 전에 다음을 확인한다.

1. `생활` 936건이 변경 대상에 포함되지 않는지 확인
2. `기타`와 `생숙`의 대상 건수가 각각 예상값과 일치하는지 확인
3. 복수 신고 건물은 신고 행 수가 아니라 고유 건물 ID 기준으로 처리되는지 확인
4. 실제 실행 후 `생활·일반·기타·생숙·NULL` 건수를 다시 조회
5. 통계 캐시가 무효화되고 지도 범례가 새 집계값으로 갱신되는지 확인

이 문서는 위 절차의 사전 검토용이며, 작성 시점에는 운영 DB에 어떠한 재분류 UPDATE도 수행하지 않았다.