# DB 데이터 수집 관리 정책 V.02 — 3단계 staging·승인 배치 구축 보고서

## 1. 단계 판정

- 대상 단계: 개발 DB staging·승인 배치 구조 구축
- 판정: 통과
- 개발 DB 변경: staging·승인·실행시도 테이블 추가
- 운영 DB 변경: 없음
- 운영 배포: 없음
- 기존 운영 동기화 변경: 없음
- 기존 `lodging_import_staging` 관리자 경로 변경: 없음

## 2. 구축 범위

개발 DB에 다음 영역을 분리했다.

1. 원본 배치
   - 원본 종류
   - 원본 파일명·확장자·SHA-256
   - 원본 기준일
   - 원본 파일 본문
   - 전체·파싱·검증통과·검토필요 행 수
2. 원본 행 staging
   - 원본 행 전체 JSON
   - 관할기관 코드·원문 관리번호
   - 기존 원장과 호환되는 관리번호
   - 원문 업태·서비스 분류·기존 서비스 분류
   - 원문 상태·상태 버킷
   - 도로명·지번 주소
   - 검토 사유와 변경 종류
3. 승인 배치
   - 승인 키와 동일 배치 재실행에 사용하는 실행번호
   - 생성 관리자·승인 관리자·승인 시각
   - 승인 시점에 고정한 변경 행과 전체 payload
4. 실행 시도
   - 같은 실행번호 아래 증가하는 시도 번호
   - dry-run/apply 실행 종류
   - 시작·heartbeat·종료 시각
   - 성공·실패 결과와 오류

`apply` 실행 종류와 `applied` 상태는 7단계 운영 반영을 위한 스키마 계약만
예약했다. 3단계 코드에는 운영 원장을 변경하는 apply 경로가 없다.

## 3. 원본과 중복 보존

- 지원 원본은 정책에 고정된 정부 숙박 8종이다.
- 같은 원본·기준일·파일 해시는 같은 배치 키를 사용한다.
- 같은 파일을 다시 올리면 새 배치나 새 행을 만들지 않고 기존 배치를 반환한다.
- 같은 배치에서 snapshot key가 반복되면 어느 행도 삭제하지 않는다.
- 중복 원문 행은 모두 `review_required`로 보존하고 자동 승인 대상에서 제외한다.
- 원본 파일과 원본 행 JSON을 함께 보존해 이후 파서 수정 때 재현할 수 있다.

## 4. 기존 원장 비교

staging 저장 시 기존 `lodging_registry`를 읽기 전용으로 비교해 다음 변경 종류를
고정한다.

- `new`: 기존 관리번호 없음
- `changed`: 상호·주소·업태 또는 복수 필드 변경
- `status_change`: 상태만 변경
- `unchanged`: 기존 원장과 동일
- `review_required`: 식별자·분류·상태 오류 또는 배치 내 중복

기존 importer가 사용하는 `RURAL`, `AIRBNB`, `PENSION`, `CAMPING`, `HANOK`
접두어와 숙박업 관리번호 원문 규칙을 유지해 기존 신고를 신규로 오인하지 않는다.

승인 배치에는 변경 없는 행과 검토 필요 행을 넣지 않는다. 승인 payload에는
식별자·분류·상태·주소·원본 JSON을 고정해 승인 후 원본이 바뀌어도 승인 내용이
변하지 않게 한다.

## 5. 승인과 실행 안전장치

- 승인 배치 생성자와 승인자는 실제 관리자 ID가 필수다.
- 승인 행은 승인 배치 생성 트랜잭션 안에서 고정된다.
- 승인 동작은 승인 상태만 기록하고 기존 숙박 원장을 변경하지 않는다.
- dry-run은 승인 완료 배치에서만 실행할 수 있다.
- 동시 dry-run은 차단하고, 5분 이상 heartbeat가 없는 시도만 실패로 회수한다.
- 실패·재시도는 새 승인번호를 만들지 않고 같은 실행번호 아래 시도 번호를
  증가시킨다.
- 각 staging 쓰기 직전에 현재 연결과 운영 DB 연결의 실제 서버·DB 지문을
  비교한다. 같으면 작업을 차단한다.

Replit 개발 워크스페이스에서도 배포 관련 환경 플래그가 production 값일 수 있어,
환경변수 이름만으로 개발·운영을 판정하지 않는다. 실제 DB 연결 대상 비교를
최종 안전장치로 사용한다.

## 6. 개발 DB 통합 검증

실제 개발 DB에서 기존 정상 신고를 기준으로 상태변경 CSV를 만들어 검증했다.

- 동일 파일 재업로드: 기존 배치 재사용
- 기존 신고 비교: `status_change` 판정
- 승인 배치: 실제 관리자 생성·승인
- dry-run 2회: 동일 실행번호 1개 유지
- 실행 시도: 시도 번호 1, 2로 누적
- dry-run 결과: `applied=false`
- `lodging_registry` 전체 건수: 전후 동일
- 기준 신고 영업상태: 전후 동일
- 테스트 staging·승인 데이터: 검증 후 삭제

개발 DB와 운영 DB는 실제 연결 지문과 원장 건수로 서로 다른 DB임을 확인했다.
운영 DB는 읽기 전용 비교만 수행했으며 어떤 테이블도 변경하지 않았다.

## 7. 테스트 결과

- staging·데이터 계약·기존 분류·기존 관리자 경로 회귀 테스트 통과
- Python 문법 검사 통과
- diff 공백 검사 통과
- 개발 DB 스키마 초기화 및 앱 기동 통과
- 브라우저 콘솔 오류 없음

기존 전체 API 워크플로에는 이번 단계와 무관한 회원 통계, 객실 만료 알림,
건물전체 입지 계산 항목의 선행 실패가 남아 있다. 3단계 관련 테스트와 앱 기동에는
영향이 없으며, 이 보고서는 해당 기존 실패를 통과로 간주하지 않는다.

## 8. 3단계 통과 조건

- 원본 파일 해시·기준일·원본 종류 보존
- 8종 공통 staging 행과 원문 JSON 보존
- 동일 파일 재업로드 멱등성
- 배치 내 중복 원문 보존 및 자동 승인 제외
- 기존 원장과 신규·수정·상태변경·동일 판정
- 검증 통과 변경분만 승인 payload로 고정
- 관리자 생성·승인 감사정보 강제
- 동일 실행번호 기반 dry-run 재시도 이력
- 운영 DB 직접 실행 차단
- staging·승인·dry-run 과정의 기존 원장 무변경
- 기존 운영 동기화와 기존 관리자 업로드 경로 유지
- 운영 배포·운영 DB 반영 없음

위 조건을 모두 충족했으므로 3단계를 통과로 판정한다. 다음 단계에서는 실제 정부
8종 원본 파일을 이 구조에 적재해 원본별 파싱·필드 품질·보류 사유와 예상 변경
건수를 검증한다.