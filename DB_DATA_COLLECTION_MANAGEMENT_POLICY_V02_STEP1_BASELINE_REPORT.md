# DB 데이터 수집 관리 정책 V.02 — 1단계 기준선 보고서

기준일: 2026-09-03 (KST)

## 판정

**1단계 Go**

- 개발·운영 DB 연결 및 핵심 테이블 존재 확인
- 운영 데이터 변경 없이 읽기 전용 기준선 집계 완료
- 실제 실행 중인 수집 프로세스 없음
- 기존 사용자 smoke 검사 통과
- 핵심 API 검사 통과
- 발견된 상태·보안·쿼터 경고를 분리하고 재발 방지 반영

이 판정은 2단계 데이터 계약·분류 규칙 확정으로 진행할 수 있다는 뜻이며,
운영 DB에 신규 정책 데이터를 반영하거나 기존 운영 수집을 중단한다는 뜻은 아니다.

## 핵심 수량

| 항목 | 개발 DB | 운영 DB |
|---|---:|---:|
| 숙박 원장 | 120,123 | 128,795 |
| 건물 마스터 | 53,872 | 83,712 |
| 실거래 | 17,223 | 18,506 |
| 건물 등록 요청 | 5 | 21 |
| 실거래 실패 큐 | 0 | 11,521 |

운영 실거래 최신 계약일은 2026-08-31, 개발은 2026-08-27이다.

## 운영 숙박 원장 상태

| 상태 | 건수 |
|---|---:|
| 영업/정상 | 79,545 |
| 폐업 | 48,907 |
| 취소/말소/만료/정지/중지 | 173 |
| 휴업 | 170 |

## 경고 분해 및 해소

### 실거래 실패 큐

- 운영 실패 11,521건은 모두 과거 HTTP 429 기록이다.
- 최초 기록은 2026-07-27, 마지막 시도는 2026-08-24다.
- 데이터 손상이나 현재 실행 중 장애가 아니라 API 쿼터로 미처리된 재시도 대기분이다.
- 기존 데이터는 보존하고 별도 저우선순위 재시도 대상으로 유지한다.

### 상가정보 실행 상태

- DB에는 2026-08-29 시작 후 `running`으로 남은 상태가 있으나 실제 프로세스는 없다.
- 상태 API는 5분 이상 하트비트가 없으면 `stale`로 판정하여 중복 실행 잠금을 해제한다.
- 운영 수집을 임의로 재시작하거나 상태 행을 삭제하지 않았다.

### 실거래·건축정보 과거 실패

- 실거래 최근 수집은 3시간 제한 초과 기록이다.
- 과거 백필은 DB SSL 연결 종료 기록이다.
- 건축정보 채우기는 DB deadlock 기록이다.
- 현재 코드에는 run_id 펜싱, stale 판정, 제한 재접속 및 재처리 보호가 있다.

### 오류문 인증값 노출 가능성

- 원문 키만 가리던 경로가 URL 인코딩된 표현을 놓칠 수 있었다.
- 원문·단일/이중 URL 인코딩·알려진 인증 쿼리 파라미터를 공통 마스킹하도록 수정했다.
- 마스킹 단위 테스트와 동기화 경로 테스트를 추가했다.

### 관광펜션 API 쿼터

- 기존에는 확인된 2,000회 전부가 정기 수집에 배정되어 복구 여유가 없었다.
- 정책에 맞춰 정기 1,600회, 수동 복구 400회로 분리했다.

## 검증 결과

- 마스킹·정기 동기화 테스트: 32개 통과
- Python 문법 검사 통과
- smoke 검사 통과
- 앱 재시작 성공
- 브라우저 화면 및 콘솔에 명백한 회귀 없음

## 2단계 진입 조건

2단계에서는 문서와 코드가 공유할 데이터 계약을 확정한다.

- 정부 숙박 CSV 8종의 원본 식별자와 상태 계약
- 서비스 분류와 건축물 용도의 분리
- 복수 인허가 보존
- 휴업·폐업·취소의 목록·통계 노출 규칙
- staging 검증 실패 시 운영 반영 차단 조건

3단계 개발 DB staging 구축 전에는 Max 사용 필요성과 예상 효과·비용을 다시 안내한다.