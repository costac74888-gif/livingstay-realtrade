# DB 데이터 수집 관리 정책 V.02 — 2단계 데이터 계약 확정 보고서

## 1. 단계 판정

- 대상 단계: 2단계 데이터 계약·분류 규칙 확정
- 운영 배포: 없음
- 개발·운영 DB 변경: 없음
- 외부 API 호출: 없음
- 다음 단계 자동 진행: 하지 않음

## 2. 정부 숙박 원본 범위

포함 원본은 다음 8종으로 고정한다.

1. 관광숙박업
2. 관광펜션업
3. 농어촌민박업
4. 숙박업
5. 외국인관광도시민박업
6. 일반야영장업
7. 자동차야영장업
8. 한옥체험업

전문휴양업은 숙박 서비스 원장에서 제외한다.

## 3. 필드 분리

다음 값은 서로 대체하지 않고 독립적으로 보존한다.

- 원본 종류: 8종 중 어느 정부 원본에서 왔는지
- 원문 업태: 정부 원본에 기록된 업태 문구
- 서비스 분류: 지도·검색·통계에서 사용하는 표준 분류
- 건축물 용도: 건축물대장 등에서 확인한 독립된 건물 용도
- 원문 영업상태와 상세 영업상태
- 원본 기준일과 데이터 갱신시점

사진, 좌표, 상가, 중개 데이터는 원장 적재 승인 조건으로 사용하지 않는다.

## 4. 서비스 분류 계약

허용값은 다음과 같다.

- 관광숙박
- 일반숙박
- 생활숙박
- 에어비앤비
- 농어촌민박
- 캠핑
- 한옥
- 미분류

주요 변환 규칙:

- 관광숙박업·관광펜션업·관광호텔·휴양콘도미니엄업 → 관광숙박
- 여관업·여인숙업·일반호텔·숙박업 기타 → 일반숙박
- 숙박업(생활) → 생활숙박
- 외국인관광도시민박업 → 에어비앤비
- 농어촌민박업 → 농어촌민박
- 일반야영장업·자동차야영장업 → 캠핑
- 한옥체험업 → 한옥
- 업태 공백 → 미분류·관리자 확인
- 알 수 없는 비공백 업태 → 자동 승인하지 않고 보류

기존 `master_buildings.lodging_type` 값은 호환을 위해 `관광`, `일반`,
`생활`, `에어비앤비`, `농어촌민박`, `캠핑`, `한옥`을 유지한다.

## 5. 상태와 노출 계약

| 원문 상태 | 기본 목록 | 활성 통계 | 처리 |
|---|---|---|---|
| 영업/정상 | 표시 | 포함 | 활성 |
| 휴업 | 표시 | 제외 | 원장 보존 |
| 폐업 | 제외 | 제외 | 원장 보존 |
| 취소·말소·만료·정지·중지 | 제외 | 제외 | 원장 보존 |
| 제외·삭제·전출 | 제외 | 제외 | 원장 보존 |
| 공백·알 수 없는 상태 | 제외 | 제외 | 관리자 확인 보류 |

상태 원문은 덮어쓰지 않고, 목록·통계 판단용 상태 버킷만 별도로 계산한다.

## 6. 식별자와 중복 계약

- 원장 upsert 키: 원본 종류 + 관할기관 코드 + 관리번호
- staging 스냅샷 키: 원본 종류 + 기준일 + 관할기관 코드 + 관리번호
- 같은 관리번호라도 원본 또는 관할기관이 다르면 별도 인허가로 보존
- 같은 스냅샷 키가 반복될 때만 같은 원본 행으로 판단
- 동일 주소·사업장명은 인허가 중복 삭제 기준으로 사용하지 않음
- 하나의 건물에 여러 인허가가 연결되어도 원장 행은 모두 보존

기준일이 없거나 잘못된 행은 임의 날짜를 넣지 않고 관리자 확인 대상으로
보류한다.

## 7. 실패·보류 계약

- 주소 정규화 실패: 원장은 보존하고 건물 연결만 보류
- 주소 후보가 복수인 경우: 자동 연결하지 않고 보류
- 건물 연결 실패: 원장은 보존하고 미연결 상태로 보류
- 알 수 없는 업태·상태: 자동 승인하지 않고 보류
- API·파일 읽기 실패: 해당 원본 배치만 실패 처리하고 기존 정상 원장은 유지
- 다른 원본의 성공 결과로 실패한 원본 상태를 덮어쓰지 않음

## 8. 현재 구현과의 차이

- 기존 `숙박업` API 수집 대상은 생활숙박과 확인된 일반숙박 업태 중심이며,
  정부 CSV 8종 계약 전체를 수집하는 구현은 아니다.
- 현재 staging 파일 경로는 에어비앤비·농어촌민박·한옥 중심으로,
  8종 공통 staging 계약은 3단계에서 구축해야 한다.
- 현재 원장 DB는 원본 종류·기준일을 독립 컬럼으로 완전히 표현하지 않는다.
  3단계에서는 기존 운영 호환성을 유지한 채 staging 스키마에서 먼저 검증한다.
- 기존 지도·API의 내부 분류값은 즉시 변경하지 않고 호환 매핑으로 유지한다.

이 차이는 2단계 실패가 아니라 3단계 구현 범위다. 2단계에서는 계약을
`lodging_data_contract.py`의 순수 규칙으로 고정해 이후 구현의 기준점으로 삼는다.

## 9. 2단계 통과 조건

- 8종 포함 및 전문휴양업 제외가 코드와 문서에서 일치
- 원문 업태·서비스 분류·건축물 용도가 분리
- 상태별 기본 목록·활성 통계 규칙이 테스트로 고정
- 원장 키와 기준일별 staging 키가 분리
- 복수 인허가와 미연결 원장 보존 규칙이 명시
- 기존 숙박 분류·CSV importer 회귀 테스트 통과
- 운영 DB 변경·운영 배포 없음

위 조건을 충족하면 2단계를 통과한 것으로 판정한다. 3단계는 별도 승인 후
개발 DB에만 staging 구조를 구축하며, 운영 DB에는 반영하지 않는다.