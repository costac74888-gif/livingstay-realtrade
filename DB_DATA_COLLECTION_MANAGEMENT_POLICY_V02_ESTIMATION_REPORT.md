# 숙박·건물 데이터 정책 V.02 단계별 작업 및 최종 데이터 추정보고서

**작성일:** 2026-09-03  
**기준 정책:** `DB_DATA_COLLECTION_MANAGEMENT_POLICY_V01.md`의 V.02 내용  
**보고서 성격:** 실제 구현 전 작업순서·소요시간·최종 데이터 규모 추정  
**현재 상태:** 1단계 작업 시작 전

## 1. 결론

다음 10단계를 최종 작업순서로 확정한다.

1. 무중단 안전망·기준선 고정
2. 데이터 계약·분류 규칙 확정
3. 개발 DB staging·승인 배치 구축
4. 정부 CSV 8종 수집·검증
5. 개발 관리자 검증 화면 구축
6. 운영 관리자 화면 전환
7. 승인 배치 운영 반영
8. 병행 운영 및 결과 비교
9. 관찰 기간 후 최종 전환
10. 전환 완료 후 운영 모니터링

이 순서는 단순한 작업 목록이 아니라 **순차 승인 게이트**다. 앞 단계의 통과 조건을 충족하지 못하면 다음 단계로 진행하지 않는다.

특히 다음 원칙을 전체 10단계에 적용한다.

> 정책 변경 중 기존 사용자 기능과 정상 운영 기능을 정지시키지 않는다. 정책 적용 완료 후 기존 기능의 회귀가 없어야 하며, 문제가 발생하면 해당 변경만 격리하고 마지막 정상 상태로 즉시 복구한다.

## 2. 현재 작업 상태

현재까지 완료된 것은 정책 문서와 추정계획 작성뿐이다.

- DB 스키마 변경: 없음
- `lodging_registry` 변경: 없음
- `master_buildings` 변경: 없음
- `transactions` 변경: 없음
- 정부 CSV 신규 적재: 없음
- 운영 DB 반영: 없음
- 관리자 버튼 동작 변경: 없음
- 운영 동기화 워크플로 중단: 없음

따라서 이 보고서의 수치는 현재 데이터를 기준으로 한 추정치이며, 실제 1단계 실행 시 읽기 전용 기준선 조사 결과에 따라 보정한다.

## 3. 단계별 작업·소요시간 추정

### 3.1 1단계: 무중단 안전망·기준선 고정

**예상 소요:** 실제 작업 기준 1~2영업일  
**운영 중단:** 없음  
**DB 쓰기:** 원칙적으로 없음. 기준선 기록을 위한 읽기 전용 조회만 수행  
**운영 반영:** 없음

작업 내용:

1. 개발·운영 DB의 주요 테이블 건수와 상태별 건수를 기록한다.
2. 관리번호·원본 종류·주소·영업상태별 중복을 확인한다.
3. 관리자 화면 버튼과 서버 라우트·실행 스크립트·대상 DB를 매칭한다.
4. 운영 자동 동기화 워크플로와 운영 DB 직접 변경 경로를 목록화한다.
5. 검색·지도·상세·실거래·숙박 목록·통계·매물·사진·인증의 기준선 테스트를 저장한다.
6. 현재 정상 배치, 백업, 복구 가능한 지점을 확인한다.
7. 변경 중 금지할 작업과 중단 기준을 확정한다.

세부 예상:

| 작업 | 예상 시간 |
|---|---:|
| DB 기준 건수·상태·중복 조회 | 2~4시간 |
| 관리자 버튼·라우트·워크플로 매칭 | 3~5시간 |
| 핵심 기능 기준선 테스트 | 2~4시간 |
| 백업·복구 가능 지점 확인 | 1~3시간 |
| 결과 정리·1단계 판정 | 1~2시간 |
| **합계** | **9~18시간** |

1단계의 최종 산출물:

- 개발·운영 기준선 수치표
- 현재 운영 직접 변경 경로 목록
- 관리자 버튼별 실행 대상표
- 핵심 기능 기준선 테스트 결과
- 마지막 정상 배치·복구 지점
- 다음 단계 진행 여부를 판단하는 Go/No-Go 기록

1단계 통과 전에는 다음을 하지 않는다.

- 운영 DB에 데이터 적재
- 기존 원장 상태 변경
- 운영 관리자 버튼 비활성화
- 운영 워크플로 중단 또는 교체
- 정부 CSV 신규 반영

### 3.2 나머지 단계의 예상 소요

아래 시간은 1단계 조사 결과와 실제 CSV 형식·기존 코드 상태에 따라 조정한다.

| 단계 | 작업 | 예상 소요 |
|---:|---|---:|
| 1 | 무중단 안전망·기준선 고정 | 1~2영업일 |
| 2 | 데이터 계약·분류 규칙 확정 | 1~2영업일 |
| 3 | 개발 DB staging·승인 배치 구축 | 3~6영업일 |
| 4 | 정부 CSV 8종 수집·검증 | 3~6영업일 |
| 5 | 개발 관리자 검증 화면 구축 | 2~5영업일 |
| 6 | 운영 관리자 화면 전환 | 2~4영업일 |
| 7 | 승인 배치 운영 반영 | 2~4영업일 |
| 8 | 병행 운영 및 결과 비교 | 2~4영업일 |
| 9 | 관찰 기간 후 최종 전환 | 3~7일 관찰 + 1~2영업일 |
| 10 | 전환 완료 후 운영 모니터링 | 초기 1~2영업일 구축 후 상시 |

예상 총기간:

- 실제 개발·검증 작업: 약 **19~37영업일**
- 무중단 관찰 기간: 최소 **3~7일**
- 보수적 전체 일정: 약 **5~8주**

이 일정은 단계 간 병렬 처리를 전제로 하지 않는다. 무중단 원칙을 우선하여 한 단계의 검증이 끝난 뒤 다음 단계로 진행하는 일정이다.

## 4. 단계별 완료 조건

### 4.1 1단계 완료 조건

- 변경 전 개발·운영 기준선 저장
- 현재 운영 직접 변경 경로 확인
- 핵심 사용자·관리자 기능 기준선 통과
- 백업 또는 마지막 정상 배치 확인
- 운영 중단 없이 복구 가능한 방법 확인

### 4.2 2~5단계 완료 조건

- 기존 API 응답과 화면 필드 호환
- 8종 CSV staging 적재 가능
- 분류·상태·중복 검증 가능
- 승인되지 않은 데이터가 운영 DB에 들어가지 않음
- 개발 관리자에서 원문과 변경분 비교 가능
- 문제가 발생해도 기존 운영 서비스를 계속 제공

### 4.3 6~8단계 완료 조건

- 운영 화면에서 전환 대상 버튼이 보임
- 대체 경로 검증 전에는 버튼을 비활성화하지 않음
- 대체 경로 검증 후 버튼은 숨기지 않고 비활성화
- 버튼 상태에 `개발에서 동기화 중` 표시
- 서버 직접 호출도 운영에서 실행되지 않음
- 승인 배치가 운영에 멱등적으로 반영됨
- 운영 반영 후 핵심 기능이 기준선과 동일하게 동작

### 4.4 9~10단계 완료 조건

- 최소 3~7일 또는 여러 차례 정기 배치에서 오류·누락·중복 없음
- 개발/staging과 운영의 승인 데이터 차이가 설명 가능함
- 기존 운영 직접 수집을 중단해도 데이터 갱신 공백이 없음
- 마지막 정상 배치로 복구하는 절차가 검증됨
- 사용자 기능의 오류율·응답시간·검색 결과에 회귀가 없음

## 5. 최종 데이터 결과 추정

### 5.1 현재 확인된 기준 수치

현재까지 확인된 수치는 다음과 같다.

| 구분 | 예상 건수 |
|---|---:|
| 개발 `lodging_registry` | 120,123건 |
| 운영 `lodging_registry` | 128,795건 |
| 양쪽 기존 유효 데이터 합집합 추정 | 약 134,668건 |
| 새 정부 CSV 8종 원본 행 | 약 143,798건 |

원본 행 수와 최종 원장 행 수는 동일하지 않다. 관리번호 중복, 필수 식별정보 오류, 완전 중복, 파싱 실패, 보류 행이 분리되기 때문이다.

### 5.2 최종 숙박 원장

정책 V.02를 적용한 최종 `lodging_registry` 규모는 다음 범위로 추정한다.

```text
약 143,500~143,800건
```

현재 수치 대비 증감 추정:

| 비교 기준 | 예상 증가량 |
|---|---:|
| 개발 DB 120,123건 대비 | 약 23,377~23,677건 |
| 운영 DB 128,795건 대비 | 약 14,705~15,005건 |
| 기존 양쪽 합집합 134,668건 대비 | 약 8,832~9,132건 |

위 범위는 staging dry-run 전의 추정치다. 최종 반영 건수는 실제 원본별 관리번호 중복과 오류를 확인한 뒤 확정한다.

### 5.3 최종 분류 결과

최종 서비스 분류는 다음처럼 유지한다.

| 서비스 분류 | 포함 대상 |
|---|---|
| 관광숙박 | 관광숙박업, 관광호텔, 휴양콘도미니엄업 |
| 일반숙박 | 여관업, 여인숙업, 일반호텔, 숙박업 기타 |
| 생활숙박 | 숙박업(생활) |
| 미분류 | 업태 공백 또는 관리자 확인 필요 행 |

금지 사항:

- `기타숙박` 서비스 분류 생성
- 관광호텔을 별도 서비스 용도로 분리
- 휴양콘도미니엄업을 별도 서비스 용도로 분리
- 서비스 분류로 건축물대장 `building_use_type` 변경

관광호텔과 휴양콘도미니엄업은 서비스 분류상 관광숙박에 포함하지만, 원장과 상세 화면에서 정부 원문 업태는 표시할 수 있다.

### 5.4 최종 상태 결과

정확한 상태별 건수는 1단계 기준선 조회와 4단계 staging dry-run 후 확정한다.

정책상 최종 결과는 다음과 같다.

| 상태 | 원장 | 기본 목록 | 활성 통계 |
|---|---|---|---|
| 영업/정상 | 보존 | 표시 | 포함 |
| 휴업 | 보존 | 표시·휴업 배지 | 제외 또는 별도 집계 |
| 폐업 | 보존 | 기본 제외 | 제외 |
| 취소·말소 | 보존 | 기본 제외 | 제외 |
| 공백·오류 | 보류 보존 | 기본 제외 | 제외 |

따라서 최종 원장 총건수와 다음 수치는 서로 다르게 관리한다.

- 전체 원장 건수
- 활성 영업 신고 수
- 휴업 신고 수
- 폐업·취소·말소 이력 수
- 실제 사업장 추정 수
- 연결 건물 수
- 건물 미연결 신고 수

### 5.5 복수 인허가 결과

동일 주소·동일 건물이라도 관리번호가 다르면 최종 원장에 모두 남는다.

예상 최종 데이터에는 다음이 동시에 존재할 수 있다.

```text
한 건물
├─ 관광숙박업 신고
├─ 관광호텔 신고
└─ 휴양콘도미니엄업 신고
```

이 경우:

- 원장 신고 건수: 관리번호별로 계산
- 상세 화면: 인허가별로 표시
- 관광숙박 통계: 중복 제거 기준 별도 적용
- 건물 수: `master_buildings.id` 기준

### 5.6 최종 반영되지 않는 데이터

다음 데이터는 숙박 원장 최종 건수에 포함하지 않는다.

- 전문휴양업
- 유효 관리번호가 없는 원본 오류 행
- 필수 식별정보가 훼손된 행
- 관리자 확인 전의 업태 공백 행
- 승인되지 않은 staging 보류 행
- 사진·좌표·상가정보 API만 실패한 숙박 신고를 제외하는 방식의 임의 필터

실거래 데이터는 `transactions` 별도 원장으로 유지하며, 숙박 CSV 최종 건수에 합산하지 않는다. 사진·지도·상가·중개 데이터도 각각 별도 보완 데이터로 관리한다.

## 6. 개발 DB와 운영 DB의 최종 관계

최종적으로 개발·운영은 전체 DB를 복사하는 방식이 아니라 승인된 논리적 변경분을 공유한다.

```text
정부 CSV 8종
        ↓
개발/staging 원본·검증
        ↓
승인 배치
        ├─ 개발 기준 데이터
        └─ 운영 승인 데이터
```

운영 DB에 반영되는 것은:

- 승인된 신규 관리번호
- 승인된 기존 관리번호 변경분
- 확인된 휴업·폐업·취소 상태 변경
- 확정된 건물 연결 변경

운영 DB에 반영하지 않는 것은:

- 개발 DB 전체 복사
- 승인되지 않은 보류 행
- 단순 API 실패를 근거로 한 삭제
- 새 매칭이 확정되지 않은 기존 건물 연결 해제

최종적으로 승인된 숙박 원장 논리 데이터는 개발·운영에서 일치해야 한다. 단, staging 이력·검증 로그·운영 감사 로그는 각 환경의 역할에 따라 별도로 보존한다.

## 7. 1단계 실행 후 예상 결과

1단계만 완료했을 때의 데이터 결과는 다음과 같다.

- `lodging_registry` 건수: 변경 없음
- `master_buildings` 건수: 변경 없음
- `transactions` 건수: 변경 없음
- 운영 원장 상태: 변경 없음
- 운영 서비스: 계속 제공
- 관리자 화면: 변경 없음
- 운영 동기화 워크플로: 계속 유지

1단계의 결과는 데이터 적재 결과가 아니라 **안전 기준선과 복구 기준의 확정 결과**다.

## 8. 1단계 중단 기준

다음 중 하나라도 발생하면 1단계 결과를 확정하지 않고 중단한다.

- 개발·운영 기준 수치를 재현할 수 없음
- 운영 DB 조회가 사용자 서비스에 영향을 줄 가능성이 있음
- 핵심 기능 기준선 테스트가 실패함
- 운영 직접 변경 경로를 식별하지 못함
- 마지막 정상 배치나 복구 지점을 확인하지 못함
- 기존 운영 데이터에 쓰기가 발생함
- 동기화 워크플로가 예기치 않게 중단됨

중단 시에도 사용자 서비스를 유지하고, 현재 운영 상태를 변경하지 않는다.

## 9. 최종 판단

이 10단계 순서는 최종 순서로 사용한다. 다만 각 단계는 독립적인 안전 게이트이며, 작업 속도보다 다음 원칙을 우선한다.

1. 기존 기능 무중단
2. 데이터 변경 전 기준선 확보
3. 대체 경로 검증 전 기존 경로 중단 금지
4. 운영 반영 전 dry-run
5. 문제 발생 시 즉시 격리·복구
6. 관찰 기간 중 회귀가 없을 때만 최종 전환

따라서 지금 시작할 작업은 1단계의 읽기 전용 기준선 조사이며, 1단계 완료 전에는 실제 정책 변경 작업으로 간주하지 않는다.