# 홈앤스테이 — 운영 원칙 & 회귀방지 가이드

이 문서는 실제로 발생했던 사고 사례를 근거로 세운 원칙입니다. 리플릿 에이전트에게
작업을 지시하기 전에, 관련 영역의 원칙을 먼저 붙여넣고 시작하면 같은 실수가
반복되는 걸 막을 수 있습니다.

---

**이 문서는 살아있는 문서입니다.** 버그를 수정하거나 새로운 패턴을
발견하면, 작업을 마무리하는 표준 절차(재시작+git push)에 다음을
포함할 것:
1. 이번에 고친 문제가 기존 영역(1~6) 중 하나에 해당하면, 그 영역의
   "실제 사고 사례"에 한두 줄로 추가
2. 기존 영역 어디에도 안 맞는 새로운 유형이면, 새 영역을 만들어 추가
3. 시크릿키를 새로 등록했다면 영역6 표에 채워넣기(값은 절대
   기록하지 말고 발급기관/용도/한도만)

---

## 영역 1 — 외부 API 연동 (시크릿키·엔드포인트·응답형식)

### 원칙
1. **새 API를 붙이기 전, 반드시 실제 응답을 먼저 눈으로 확인한다.** curl로 직접
   호출해서 HTTP 상태코드·Content-Type·응답 본문 앞부분을 확인한 뒤에야 파싱
   코드를 짠다. "이 API는 XML일 것이다/JSON일 것이다"라는 추측으로 코드부터
   짜지 않는다.
2. **"왜 실패하지?" 오류가 나면 항상 실측이 먼저, 수정은 그 다음이다.** 예외
   메시지만 보고 고치지 말고, 실제 요청 파라미터·실제 응답 본문을 로그로
   남겨서 확인한 뒤 원인을 확정한다.
3. **시크릿키 이름 규칙을 통일한다**: `{서비스명}_SERVICE_KEY` 형태로. 어떤 키가
   어느 API용인지, 발급기관·일일한도·만료일을 표(영역6 참고)로 한 곳에 관리한다.
4. **Encoding/Decoding 키 혼동에 주의한다** — `requests`의 `params` 딕셔너리
   방식은 자동으로 URL 인코딩하므로 Decoding(원본) 키를 써야 한다. 직접 URL
   문자열을 조립하는 방식이면 Encoding 키를 쓴다.

### 실제 사고 사례
- **store_info_util.py JSON/XML 왔다갔다 사고**: "type=json 시 403"이라는
  틀린 진단으로 XML 파싱을 JSON 파싱으로 잘못 바꿨다가, 실측(curl 3종 비교표)
  으로 재확인 후 되돌림. → 처음부터 실측했다면 이 왕복 자체가 없었을 사고.
- **건축HUB 표제부 조회 "not well-formed" 오류**: 동일 유형(파싱 실패)이 다른
  API에서도 재발. 실측 없이 추측하면 같은 실수가 API마다 반복됨.

---

## 영역 2 — 데이터 동기화 파이프라인 (11개 sync 작업)

### 원칙
1. **모든 배치 작업은 "체크포인트 + 일일 호출 캡 + 실패 시 이어하기" 3종 세트를
   기본 탑재한다.** (`sync_permits.py`/`sync_batch.py` 패턴을 표준으로 삼고,
   새 동기화 작업은 이 틀에서 벗어나지 않는다.)
2. **여러 배치 작업이 "일일 API 호출 예산"을 공유하지 않는다.** 공유하면 한
   작업이 다른 작업의 예산을 잠식한다. 공유가 불가피하면 명시적으로 우선순위와
   쿼터 배분을 코드에 못 박는다(예: 정기동기화 우선 확보 후 남는 만큼 백필).
3. **테이블/컬럼을 바꾸는 배치 작업은 반드시 `SCHEMA_VERSION`을 함께 올린다.**
   안 올리면 fast-path 최적화 때문에 실제 DB에 반영되지 않는다.
4. **매칭 로직(주소 정규화 등)을 바꿀 때, 성능을 위해 정확성을 몰래 희생하지
   않는다.** 함수 조건을 완화(REPLACE 제거 등)하려면, 그 대가로 데이터를
   사전에 정규화해서 정확성을 유지하는 방식을 함께 설계한다.
5. **필터 조건을 바꾸면(예: buildingType 필터 완화), 이미 "완료"로 체크포인트
   찍힌 과거 구간은 소급 반영되지 않는다는 걸 명시한다.** 체크포인트 초기화 +
   재실행이 필요하면 그것도 지시에 포함한다.

### 실제 사고 사례
- **일일 호출 카운터 공유 사고**: 정기동기화·백필·재시도 큐가 `app_meta`의
  같은 키를 공유해서, 백필이 실제로 쓸 수 있는 몫이 8,000건보다 훨씬 적었음.
  → 백필 전용 카운터로 분리 후 해결.
- **umd_nm 공백 매칭 개선**: `REPLACE()` 함수를 지우고 인덱스를 태우려다,
  대신 데이터를 사전에 정규화(공백 제거)하는 마이그레이션으로 정확성을
  유지하며 성능도 개선 — 이게 올바른 패턴.
- **일반숙박시설 필터 완화 후 소급 미반영**: 필터를 고쳐도 체크포인트가
  "완료"로 남아있어 재실행 전까진 옛 데이터가 그대로였음.

### 11개 동기화 작업 — 관리 원칙
각 동기화 작업은 관리자 "데이터 동기화" 화면에서 다음을 **항상 한눈에** 볼 수
있어야 한다: 무엇을 채우는지 / 어떤 API를 쓰는지 / 체크포인트 상태 / 오늘 API
사용량(한도 대비) / 마지막 실행 결과. (이미 잘 정착된 패턴 — 새 작업 추가 시
동일 UI 재사용)

---

## 영역 3 — 회원 로그인 게이트

### 원칙
1. **핵심 열람 콘텐츠(지도·실거래·건물정보)는 절대 로그인으로 막지 않는다.**
   로그인 게이트는 "사용자가 이 정보를 계속 추적하고 싶다"는 의도를 스스로
   표현한 순간(저장·신청·제출)에만 건다.
2. **로그인 게이트를 하드/소프트로 바꿀 때, 그 기능을 호출하는 모든 진입점을
   전부 찾아서 동시에 바꾼다.** 메인페이지 위젯, 건물상세 페이지, 독립 페이지
   (예: transactions.html처럼 main.js를 공유하지 않는 페이지)를 각각 확인한다.
3. **비로그인 사용자의 기존 데이터(localStorage 등)를 하드게이트 전환 시에도
   보존한다.** 갑자기 사라진 것처럼 보이면 안 되고, 로그인 시 기존 데이터가
   이전(migrate)되는 경로를 유지한다.

### 실제 사고 사례
- **관심저장 이중 구조 발견**: "관심저장은 로그인 게이트"라고 잘못 판단했다가,
  실제로는 localStorage(비로그인) + 서버(로그인) 하이브리드였음이 밝혀짐 —
  코드를 직접 안 보고 판단하면 이런 오류가 생김.
- **하드게이트 전환 시 transactions.html 누락 위험**: main.js와 별도 파일이라
  명시적으로 지시하지 않으면 빠질 뻔한 지점.

---

## 영역 4 — 신청·승인·만료 시스템 (단지뱃지 / OTA링크 / 지역Master 공통 패턴)

### 원칙
1. **"신청→검토→승인→만료→재신청" 패턴을 쓰는 모든 기능은 동일한 컬럼
   네이밍**(`*_granted_at`, `*_expires_at`, `renewal_count`)**과 동일한 관리자
   화면 구조**(요약 카운트 + 목록 + 만료 D-day 표시)**를 그대로 복제해서 쓴다.**
   매번 새로 설계하지 않는다.
2. **"1회 한정" 같은 정책은 반드시 서버 코드로 강제한다.** 안내 문구만 있고
   서버가 막지 않으면 실제로는 무제한 재신청이 가능한 상태가 된다.
3. **관리자 화면은 "지금 활성 상태인 것"을 항상 요약으로 먼저 보여준다.**
   대기중 목록이 0건이어도 빈 화면처럼 보이지 않게, 요약 카운트(대기중/운영중/
   만료임박)를 목록 유무와 무관하게 항상 표시한다.
4. **신원 확인이 필요한 신청(외부로 나가는 링크 등)과, 자기선언으로 충분한
   신청(담당단지 등록 등)을 구분한다.** 전자는 승인 절차 필수, 후자는 즉시
   적용해도 무방.

### 실제 사고 사례
- **OTA 링크 재신청 무제한 허용 상태**: "1회 한정"이 정책 문서에만 있고 코드에
  구현이 안 되어 있었음 — 뒤늦게 발견해 `renewal_count` 컬럼과 차단 로직 추가.
- **OTA 승인 화면 "안 보이네?" 오해**: 기본값이 "대기중만 표시"라 승인 후
  화면이 비어 보였음 — 실제로는 버그가 아니었지만, 요약 카운트가 있었다면
  애초에 오해가 없었을 것.
- **운영업체 단지뱃지 신청 버튼 누락**: 백엔드 API는 완성돼 있었는데 프론트
  버튼만 없었음 — 중개사 버전과 비교해서 발견. 같은 패턴을 다른 회원 유형에
  적용할 때는 매번 "버튼까지 실제로 연결됐는지" 확인 필요.

---

## 영역 5 — 프론트엔드 공통 자산 (지도 / 색상 / 스크립트 로딩)

### 원칙
1. **스크립트 로딩 순서(defer/일반)를 바꿀 때는 관련된 모든 스크립트에
   일관되게 적용한다.** 하나만 바꾸면 실행 순서가 역전되어 새 버그가 생긴다.
2. **색상 팔레트(`LODGING_COLORS` 등)처럼 여러 파일에 중복 정의된 값은, 값이
   바뀔 때 관련 파일 전부를 동시에 갱신하도록 지시에 명시한다.** (main.js와
   transactions.html은 파일을 공유하지 않아 각자 값을 갖고 있음.)
3. **지도 기본 위치·줌 값은 코드만으로 정확히 못 맞춘다.** 값을 바꾼 뒤 반드시
   실제 스크린샷으로 확인하고, 필요하면 1~2회 추가 미세조정한다.
4. **확정된 모바일 지도 기본값은 사용자 승인 없이 절대 바꾸지 않는다.**
   현재 확정값: `lat:35.4 / lng:127.9 / level:13`
   (속초 이북 끝부분에 붙음·제주 완전 노출·부산·울산 줌컨트롤 안가림 — 2026-08-07 승인)

### 실제 사고 사례
- **Chart.js defer 적용 반쪽 사고**: Chart.js에만 `defer`를 추가하고 나머지
  스크립트는 그대로 둬서, 실행 순서가 역전되며 실거래추세 차트가 조용히
  빈 화면으로 남았던 사고.
- **transactions.html 색상 배지 불일치**: main.js의 `LODGING_COLORS`가
  업데이트됐는데 transactions.html은 옛날 3분류(생활/호텔/콘도) 매핑을 그대로
  쓰고 있어 지도와 다른 색이 표시됐던 사고.
- **모바일 지도 중심 반복 미세조정**: lat 35.8 → 35.65 → 35.4 세 번 조정 끝에
  확정. 코드값만으로 추측하지 말고 실기기 스크린샷으로 확인 후 확정할 것.

---

## 영역 6 — 시크릿키 관리 대장 (직접 채워서 유지)

| 키 이름 | 발급기관 | 용도 | 일일한도 | 만료일 | Encoding/Decoding |
|---|---|---|---|---|---|
| RTMS_SERVICE_KEY | data.go.kr | 실거래가 조회 | 10,000 (소프트캡 8,000) | | Decoding |
| BLD_SERVICE_KEY | data.go.kr | 건축HUB 표제부 | | | |
| STORE_INFO_SERVICE_KEY | data.go.kr | 상가업소 조회 | | | |
| BLD_INSPECTION_SERVICE_KEY | data.go.kr | 정기점검이력 | | | |
| DATA_GO_KR_BROKER_API_KEY | data.go.kr | 중개업소 표준데이터 | 1,000 | | |
| LODGING_SERVICE_KEY | data.go.kr | 숙박업 인허가 조회 | | | |
| KAKAO_REST_API_KEY | kakao developers | 지오코딩·지도 REST | | | |
| KAKAO_JS_KEY | kakao developers | 지도 SDK (프론트) | | | |
| KAKAO_CLIENT_SECRET | kakao developers | OAuth 로그인 | | | |
| RESEND_API_KEY | resend.com | 이메일 발송 | | | |
| JUSO_API_KEY | juso.go.kr | 도로명→지번 변환 | | | |
| VWORLD_API_KEY | vworld.kr | 공간정보 조회 | | | |
| SESSION_SECRET | 자체 생성 | Flask 세션 서명 | 해당없음 | 해당없음 | 해당없음 |

(빈 칸은 다음에 확인되는 대로 채워넣으시면 됩니다.)

---

## 전 영역 공통 — 지시·검증 습관

1. **지시문은 번호 매긴 항목으로, 하나의 복사용 코드블록에 담는다.**
2. **완료 보고는 "말"이 아니라 GitHub raw 코드 diff로 확인한다.** 번호 매긴
   항목 전부를 빠짐없이 확인하고, 일부만 확인하고 넘어가지 않는다.
3. **여러 지시가 밀렸을 가능성을 항상 의심한다.** "지시 → 완료 보고 → 다음
   지시"를 반복하다 보면 중간에 반영이 안 된 채 넘어가는 경우가 생긴다.
4. **표준 마무리 절차**: 재시작 + git push 항상 함께. 스키마 변경 시
   SCHEMA_VERSION 갱신 필수. Dev 확인 후 Prod 배포까지 확인.
5. **원칙 문서 갱신**: 작업 마무리 시 "이번 수정이 PROJECT_PRINCIPLES.md에
   추가할 만한 원칙인가?"를 스스로 판단해서, 해당되면 같이 갱신하고 커밋.
